hl.config({
  input = {
    kb_layout = "us",
    kb_options = "", -- Keep Insert and Caps Lock normal instead of inheriting Compose mappings.
    follow_mouse = 2,
    accel_profile = "flat",
  },
})
