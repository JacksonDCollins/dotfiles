[Unit]
Description=Refresh the OpenRouter usage record whenever agent usage regenerates

[Path]
# The panel runs omarchy-agent-usage-update on its own timer and on manual
# refresh; that write into the usage directory is our integration hook.
PathChanged=%h/.local/state/omarchy/agents/usage

[Install]
WantedBy=paths.target
